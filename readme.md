# Exportio Interview Task

## How to run?

Firstly add the Base Linker token to .env file:

```BASELINKER_TOKEN=your_token_here```

Run following commands:
```bash
  uv sync
```

```bash
  python manage.py runserver
```

Now the app is ready on http://localhost:8000/